name: Go CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Клонируем репозиторий
    - uses: actions/checkout@v4

    # 2️⃣ Устанавливаем Go 1.26.2
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.26.2'

    # 3️⃣ Запуск golangci-lint через официальный Action
    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v3
      with:
        version: v1.59.0

    # 4️⃣ Сборка проекта
    - name: Build
      run: go build -v ./...

    # 5️⃣ Запуск тестов
    - name: Test
      run: go test -v ./...